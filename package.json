{
  "name": "lit-benchmarks",
  "private": true,
  "version": "1.0.0",
  "description": "Benchmarks for lit-html",
  "main": "index.js",
  "scripts": {
    "prepare": "(cd runner && npm install) && (cd client && npm install) && (cd benchmarks && npm install)",
    "benchmarks": "node runner/bin/benchmark.js --root=./benchmarks",
    "build": "(cd runner && npm run build) && (cd client && npm run build)",
    "format": "(cd runner && npm run format) && (cd client && npm run format) && (cd benchmarks && npm run format)",
    "test": "(cd runner && npm run test)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PolymerLabs/lit-benchmarks.git"
  },
  "author": "The Polymer Project Authors",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/PolymerLabs/lit-benchmarks/issues"
  },
  "homepage": "https://github.com/PolymerLabs/lit-benchmarks#readme"
}
